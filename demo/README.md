# demo

> **A log, not documentation.** These files record demo and benchmark runs from an earlier
> release and are kept for the record. The current documentation is in
> [`docs/`](../docs/README.md).

`BENCHMARKS.md`, `H100_SETUP.md`, `OPTIMIZATIONS.md` and `PERF-LOG.md` are the records; nothing
here is maintained against the current release.
